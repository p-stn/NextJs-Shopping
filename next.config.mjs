/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['dkstatics-public.digikala.com','fakestoreapi.com'],
    },
    typescript: {
        ignoreBuildErrors: true,
     },
};

export default nextConfig;
